name: timers
description: Jericas timers

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  dartz: ^0.10.0
  equatable: ^2.0.3
  formz: ^0.4.0
  #bloc
  bloc: ^7.2.1
  flutter_bloc: ^7.3.0

  #hive local storage:
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  #dependency injection
  injectable: ^1.5.0
  get_it: ^7.2.0

  #MISC
  cupertino_icons: ^1.0.2
  font_awesome_flutter: ^9.1.0
  google_fonts: ^2.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  #Update applicaion ID
  flutter_application_id: ^2.0.0-dev
  #code generation
  build_runner: ^2.1.4
  injectable_generator: ^1.5.2
  json_serializable: ^5.0.2
  freezed_annotation: ^0.15.0
  freezed: ^0.15.0+1
  #test framework
  mocktail: ^0.1.4
  bloc_test: ^8.3.0


flutter_application_id:
  android:
    id: "com.project.timers"
    name: "Jerica Timers"
  ios:
    id: "com.project.timers"
    name: "Jerica Timers"
flutter:
  uses-material-design: true
  assets:
    - assets/offroad/
    - assets/otter/